package test;

import utils.StringUtils_Dalton;

//STUDENT MUST EDIT!!!:
//	  *	Change *EVERY* instance of "Skeleton"
//		BELOW TO YOUR LastName
//	  * EX: Dr. Kart would change StringUtils_Skeleton.reverse(str) to:
//			StringUtils_Kart.reverse(str)
public class StringUtilsTestsSubsetForStudents_0_STUDENT_MUST_EDIT
{
	public String reverse(String str)
	{
		return StringUtils_Dalton.reverse(str);
	}

	public boolean isPalindrome(String str)
	{
		return StringUtils_Dalton.isPalindrome(str);
	}

	public int getCount(String str, String pattern)
	{
		return StringUtils_Dalton.getCount(str, pattern);
	}

	public String rotate(String str, int delta)
	{
		return StringUtils_Dalton.rotate(str, delta);
	}

	public String shift(String str, int delta)
	{
		return StringUtils_Dalton.shift(str, delta);
	}

	public boolean areAnagrams(String str1, String str2)
	{
		return StringUtils_Dalton.areAnagrams(str1, str2);
	}
	
	public int getCodePointCount(String str)
	{
		return StringUtils_Dalton.getCodePointCount(str);
	}
	
	public int getCodePoint(String str, int codePointIndex)
	{
		return StringUtils_Dalton.getCodePoint(str, codePointIndex);
	}
	
	public String getUnicodeCharacter(String str, int codePointIndex)
	{
		return StringUtils_Dalton.getUnicodeCharacter(str, codePointIndex);
	}
}
